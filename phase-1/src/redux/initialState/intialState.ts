import { string } from "yup";

export const initialState = {
  navMenu: false,
};

export const accInitialState = {
  accMenu: false,
};

export const mobileShowPreviewInitialState = {
  mobilePreview: false,
}

export const showPreviewInitialState = {
  previewItems:{
    type:'',
    payload:''
  },
};

